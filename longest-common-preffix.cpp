#include <bits/stdc++.h>
using namespace std;


int main(){
 
/* Code here  */
vector<string> strs { "flower", "flow", "flight" };
int n=strs.size();
int m = strs[n-1].size();
vector<string> dummy ;



for(int i=strs.size()-1;i>-1;--i)
{
    for (int j= strs[i].size() - 1; j > -1; --j)
    {
   
    }
  
}

return 0;
}